{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "dbank-latest",
  "scripts": {
    "build": "dfx generate && dfx canister create --all && npm run build --workspace dbank-latest-frontend",
    "prebuild": "dfx canister create --all",
    "start": "npm start --workspace dbank-latest-frontend",
    "test": "npm test --workspace dbank-latest-frontend"
  },
  "type": "module",
  "workspaces": [
    "src/dbank-latest-frontend"
  ],
  "dependencies": {
    "dotenv": "^16.4.7"
  }
}
